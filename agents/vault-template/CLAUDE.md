# Vault

This is a shared workspace for cross-session memory. Agents can read from and write to this directory to maintain context across conversations.

## Structure

- `scratch.md` — quick notes, working memory, anything ephemeral
- `sessions/` — session handoffs and context (optional)

Keep it simple. This is a scratchpad, not an archive.
