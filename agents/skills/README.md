# Skills notes

## Naming conventions

- Skill names must be lowercase with numbers and hyphens only.
- Names must not start or end with a hyphen.
- Keep names under 64 characters.

## Compatibility metadata

Use the `compatibility` frontmatter field only when a skill requires specific
system packages, network access, or tooling beyond the default agent runtime.
